[project]
name = "pathweave-python"
version = "0.1.0"
description = "AI and external integrations (RateMyProfessor, etc.)"
readme = "README.md"
requires-python = ">=3.14.4"
dependencies = [
    "fastapi[standard]>=0.136.1",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
